var ObjectsHelpers = {
  // is the object defined?
  isDefined: function(object) {
    if (object === undefined) {
      return false;
    } else {
      return true;
    }
  }
};

module.exports = ObjectsHelpers;
